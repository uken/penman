machine:
  ruby:
    version: 2.2.3

database:
  override:
    - cd spec/dummy && bundle install && bundle exec rake db:create && bundle exec rake db:migrate

deployment:
  release:
    branch: master
    commands:
      - gem build monopoly.gemspec
      - curl http://gems.uken.com/upload -XPOST file=@$(echo penman*.gem)
